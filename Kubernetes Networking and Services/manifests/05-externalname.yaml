# ExternalName - no proxying, no endpoints. Just a CNAME to something outside.
# Name: Hemang | Enrollment number: 24bcs10209
apiVersion: v1
kind: Service
metadata:
  name: external-api
  namespace: k8s-lab
spec:
  type: ExternalName
  externalName: example.com
